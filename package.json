{
  "name": "@jwalsh/profile-dns",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "signer": "cd signer && http-server -h localhost -p 3000",
    "resolver": "cd resolver && nodemon index.js localhost 3001",
    "provider": "cd provider && http-server -h localhost -p 3002",
    "demo": "./node_modules/.bin/babel-node provider/auth.js",
    "build": "./node_modules/.bin/babel src -d lib"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "blockstack-auth": "^0.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.4",
    "babel-preset-latest": "^6.16.0",
    "nodemon": "^1.11.0"
  }
}
